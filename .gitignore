# Swift / SPM
.build/
.xcode_build/
.swiftpm/
*.xcodeproj
DerivedData/
Package.resolved

# Node (web-src + mcp-bridge)
node_modules/
mcp-bridge/dist/
web-src/dist/

# Desktop console (Electron wrapper) — commit source only, never the build output
desktop/dist/
/dist/

# Abandoned Tauri experiment (Electron was chosen) — only build artifacts remain, no source
desktop-tauri/

# macOS / editors
.DS_Store
.vscode/

# Misc
*.log

# Demo (xcodegen-generated)
Demo/.build/
Demo/SandboxServerDemo.xcodeproj/
Demo/Generated/
