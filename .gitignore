# Swift / SPM
.build/
.xcode_build/
.swiftpm/
*.xcodeproj
DerivedData/
Package.resolved

# Node (web-src + mcp-bridge)
node_modules/
mcp-bridge/dist/
web-src/dist/

# Desktop console (Electron wrapper) — commit source only, never the build output
desktop/dist/
/dist/

# Abandoned Tauri experiment (Electron was chosen) — only build artifacts remain, no source
desktop-tauri/

# macOS / editors
.DS_Store
.vscode/

# Misc
*.log

# Examples (xcodegen-generated; .build/ + *.xcodeproj already covered above)
Examples/*/.build/
Examples/*/*.xcodeproj/
Examples/*/Generated/
