#ifndef SANDBOXSERVER_SYSTEM_SQLITE_SHIM_H
#define SANDBOXSERVER_SYSTEM_SQLITE_SHIM_H

/* Bridges the OS-provided libsqlite3 into Swift as `import SandboxServerSystemSQLite`,
 * keeping the SDK free of any third-party SQLite wrapper. */
#include <sqlite3.h>

#endif /* SANDBOXSERVER_SYSTEM_SQLITE_SHIM_H */
